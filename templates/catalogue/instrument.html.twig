{% extends 'base.html.twig' %}
{% block body %}

<h1 class="titre text-center my-5">Les {{ rubrique[0].libelle }}s</h1>

<div class="d-flex justify-content-center ">
    <div class="col-8 row mb-3  ">
        {% for instrument in instruments %}
            <a class="col-6 my-4  pasDeDeco" href="{{path('app_detail',{'id':instrument.id})}}" type="button">
                <div class="card cardInstrument w-100 flex-row">
                    <div class="card-img w-50">
                        <img class="img-fluid my-4 ms-4 cardInstrument col-10" src="{{asset('instrument/'~ instrument.photo.avecFond )}}"  alt="{{ instrument.libelle }}">
                    </div>
                    <div class="card-text w-50 my-4 flex-column text-center">
                        <div class="h-75 pt-5">
                            <p class="mx-4 titreInstrument ">{{ instrument.libelle}}</p>
                            <p class="prixInstrument">{{instrument.prixHt}} €</p>
                            </div>
                        <div class="align-items h-25 me-3 ">
                            <img class="col-2 img-fluid" alt="ajouter dans mes favoris" src="{{asset('image/icone/coeurVide.png')}}">
                            <button class="btn btnAjoutPanier col-9" href="" type="button">ajouter au panier</button>
                        </div>
                    </div>
                </div>
            </a>
        {% endfor %}
    </div>
</div>
{% endblock %}