{% extends 'base.html.twig' %}
{% block body %}

 <div class="text-center my-5"><h2 class="titre"> Nos {{ rubrique[0].libelle }}</h2></div> 


     <div class="d-flex justify-content-center w-100">
        <div class=" row justify-content-between  col-10"> 
           {% for sousRubrique in sousRubriques %}
            {# <a class="btn col-4 my-3 " href="instrument/{{ sousRubrique.id}}" type="button">  #}
            <a class="btn col-4 my-3 " href="" type="button"> 
                    <div class="card  h-100">
                        <div class="card-header cardInvisible text-center">
                            <h2 class="titreSousRubrique h-25">{{ sousRubrique.libelle}}</h2>
                        </div>
                        <div class="card-img  ">
                            <img class="img-fluid col-8 h-75" alt="{{ sousRubrique.libelle}}" src="{{asset('instrument/'~ sousRubrique.photo)}}">
                        </div>
                    </div>
                </a>
           {% endfor %}
        </div>         
                                                
{# {% for test in tests %}
    {% for testss in tests.parent %}
        <p>{{ testss.libelle }}</p>

    {% endfor %}

{% endfor %} #}
{# {% for rubric in tests %}
<p >
{{ rubric.libelle }}
{% for subrubric in rubric.parent %}
<p>{{ subrubric.libelle }}</p>
{% endfor %}
</p>

		</p>
		</div>
	{% endfor %} #}

    </div>
</div>
{% endblock %}